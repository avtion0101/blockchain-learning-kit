# 区块链核心心智模型

## 一句话定义

> 区块链是一套让互不完全信任的人，在没有单一管理员的情况下，共同维护同一本账、按照同一套规则修改状态的系统。

普通数据库通常由一个机构决定谁能写入、写入顺序以及如何纠错。区块链把这些职责拆给许多参与者，并用公开验证规则决定大家接受哪一份状态。这种设计减少了对单一运营方的依赖，但代价通常是更低的吞吐量、更高的成本、更复杂的治理和更差的隐私。

## 四块拼图

1. **密码学**：哈希为数据生成指纹；数字签名证明某个私钥授权了特定消息。
2. **点对点网络**：交易和区块在节点之间传播，许多节点独立保存或验证数据。
3. **共识**：参与者对交易顺序、有效区块和当前状态形成一致看法。
4. **激励与约束**：奖励维护系统的人，并让违反规则或攻击网络付出成本。

只有“数据连成链”还不够。攻击者完全可以在自己的电脑上重算一条哈希链；真正困难的是让其他诚实节点按照共识规则接受这段伪造历史。

## 一笔交易经历了什么

```mermaid
flowchart LR
    A[用户构造交易] --> B[钱包用私钥签名]
    B --> C[交易广播到网络]
    C --> D[节点独立检查规则]
    D --> E[区块提议者选择并排序]
    E --> F[共识接受区块]
    F --> G[节点执行并更新状态]
```

以 Alice 向 Bob 转账为例：

1. 钱包构造包含接收方、金额以及链所需其他字段的交易。
2. Alice 的私钥对交易签名。私钥不应离开签名设备。
3. 节点检查签名、资金来源、序号或 UTXO、格式与其他共识规则。
4. 有效交易进入候选池，等待区块提议者选择。
5. 交易进入被网络接受的区块后，各节点按相同规则执行，得到相同的新状态。
6. 后续区块或共识投票增加交易被逆转的难度或使其达到协议定义的最终性。

这也解释了为什么“已广播”“已打包”和“已最终确认”不是同一个状态。

## 密码学分别证明什么

| 工具 | 它能帮助证明 | 它不能证明 |
|---|---|---|
| 哈希 | 输入没有发生未被发现的变化 | 输入内容在现实世界中真实 |
| 数字签名 | 对应私钥授权了这段具体数据 | 私钥背后一定是声称的现实身份 |
| Merkle 证明 | 某项数据属于一个已承诺的数据集合 | 承诺该集合的人一定诚实 |
| 共识 | 网络根据规则接受了某段历史 | 链外事实、价格或法律关系一定正确 |

## 钱包里究竟有什么

资产不是一个文件，也不实际装在钱包应用里。资产归属是区块链状态的一部分；钱包负责管理密钥、生成地址、读取链上状态、构造交易和请求签名。

可以把它理解为：

- 链上账本记录“哪些条件满足时可以花费某项资产”。
- 私钥让持有者能够生成满足其中某类条件的签名。
- 钱包是操作密钥和交易的工具。
- 丢失私钥通常不是“忘记网站密码”，网络没有中央管理员替你重置。

## Bitcoin 与 Ethereum 的两种账本模型

### Bitcoin：UTXO

UTXO 是“尚未被花费的交易输出”。新交易引用并消费旧输出，再创建新的输出。某人的可用金额可以通过满足其控制条件的 UTXO 相加得到。

把它想成现金更容易入门：花掉一张 10 元纸币支付 6 元时，会生成给商家的 6 元输出和给自己的找零输出；输入减去输出的差额可以成为手续费。这个比喻不是完整协议，但能解释为什么 Bitcoin 交易关注输入、输出与防止双花。

### Ethereum：账户状态机

Ethereum 维护账户、余额、合约代码和合约存储等全局状态。交易按照顺序触发状态转换；发送普通转账或调用合约都属于交易。EVM 是节点用于确定性执行合约代码的环境，Gas 用来计量执行和存储所消耗的网络资源。

简化比较：

| 维度 | Bitcoin | Ethereum |
|---|---|---|
| 入门模型 | 消费旧 UTXO，创建新 UTXO | 交易驱动全局状态转换 |
| 主要表达能力 | 受约束的价值转移条件 | 通用智能合约执行环境 |
| 交易顺序关注点 | 哪些输出尚未被花费 | 账户 nonce 与共享状态的执行顺序 |
| 核心学习重点 | 双花、工作量证明、确认、脚本 | 账户、Gas、EVM、合约、权益证明 |

## 智能合约是什么

智能合约是部署到链上、由交易触发的程序。每个验证节点必须能对相同输入得到相同结果，因此它不能像普通服务器程序一样随意访问网络、读取秘密或调用不确定的外部数据。

需要外部价格、天气或比赛结果时，系统依赖预言机把信息送上链。这不会神奇地保证数据真实，只是把“信任谁提供数据、如何聚合、错误时怎么办”变成协议设计的一部分。

合约公开、可组合和难以随意撤销，但也带来明显风险：错误代码可能直接控制资产；升级管理员可能拥有很大权限；公开交易可能被抢先排序；一次外部调用可能产生重入等意外控制流。

## 什么时候值得使用区块链

先问：

> 是否存在多个互不完全信任的参与者，需要共同维护状态，而且不能或不愿依赖一个中心机构？

继续检查：

- 用户是否需要独立验证，而不仅是相信运营方的接口？
- 多方是否真的需要共享写入权或无需许可地参与？
- 抗审查、可组合、公开结算或数字稀缺性是否是核心需求？
- 系统能否接受公开数据、较高费用、较慢确认和难以纠错的代价？
- 链下管理员、预言机、排序器或托管人是否又重新形成了单点信任？

如果一个可信机构完全可以高效管理全部数据，普通数据库往往更合适。

## 不存在免费的性能提升

分析一条链或扩容方案时，同时查看：

- **安全由谁提供**：攻击需要控制什么资源？
- **数据在哪里**：普通用户能否获得验证所需数据？
- **执行在哪里**：谁计算交易结果，谁能提交证明或挑战？
- **最终性在哪里**：交易最终依赖哪一层、什么假设？
- **排序由谁控制**：能否审查、延迟或重排交易？
- **升级由谁决定**：管理员、治理投票、客户端团队还是社会共识？

所谓“更快、更便宜”通常意味着批量处理、减少数据、使用更强硬件、依赖额外角色或把安全性结算到另一层。研究的任务不是先判断好坏，而是准确指出代价转移到了哪里。

## 七个常见误解

1. **“钱包保存着币”**：钱包主要控制密钥，资产记录在链上状态中。
2. **“哈希就是加密”**：哈希通常不可逆，用于指纹；加密设计为持有密钥者可以还原明文。
3. **“上链信息一定真实”**：链能保证规则内的数据一致性，不能自动保证链外输入真实。
4. **“区块链绝对不可篡改”**：更准确地说，修改已接受历史会被检测，并受到共识和成本约束。
5. **“智能合约就是法律合同”**：它首先是程序；法律效力取决于司法辖区与具体关系。
6. **“去中心化意味着没人控制”**：控制可能分布在验证者、开发者、治理、前端、预言机和管理员之间。
7. **“代币涨价代表协议成功”**：价格、使用量、收入、安全性和代币价值捕获是不同问题。

下一步：运行 [`labs/foundations/`](./labs/foundations/) 的实验，用输出验证这里的概念，而不是只背定义。

